import React from "react";
import CrudPage from "../../components/common/CrudPage";
import { goalConfig } from "../../config/moduleConfigs";

const Page = () => <CrudPage config={goalConfig} />;

export default Page;
