import React from "react";
import CrudPage from "../../components/common/CrudPage";
import { departmentConfig } from "../../config/moduleConfigs";

const Page = () => <CrudPage config={departmentConfig} />;

export default Page;
