import React from "react";
import CrudPage from "../../components/common/CrudPage";
import { branchConfig } from "../../config/moduleConfigs";

const Branches = () => <CrudPage config={branchConfig} />;

export default Branches;
