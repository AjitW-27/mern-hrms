import React from "react";
import CrudPage from "../../components/common/CrudPage";
import { onboardingConfig } from "../../config/moduleConfigs";

const Page = () => <CrudPage config={onboardingConfig} />;

export default Page;
