import React from "react";
import CrudPage from "../../components/common/CrudPage";
import { projectConfig } from "../../config/moduleConfigs";

const Page = () => <CrudPage config={projectConfig} />;

export default Page;
