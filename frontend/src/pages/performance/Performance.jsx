import React from "react";
import CrudPage from "../../components/common/CrudPage";
import { performanceConfig } from "../../config/moduleConfigs";

const Page = () => <CrudPage config={performanceConfig} />;

export default Page;
